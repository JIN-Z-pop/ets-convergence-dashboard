import { SectionTitle } from 'ets-editorial';

export const Default = () => <SectionTitle>Banking Simulator</SectionTitle>;

export const Longer = () => <SectionTitle>Allocation Method Evolution (GF → BM → Auction)</SectionTitle>;
